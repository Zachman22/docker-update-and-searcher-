# External Dependencies

This directory contains third-party header-only libraries.

## nlohmann/json

For JSON parsing we'll use the CMake FetchContent to automatically download it.

The json.hpp header is fetched automatically during build.
